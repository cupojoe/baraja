# Baraja

A static technical design microsite for an offline-ready, multi-user deck of cards app.

Open `docs/index.html` in a browser to view the site locally.

The site is configured for GitHub Pages through `.github/workflows/pages.yml`, which publishes the static files in `docs/`. In the repository's Pages settings, set the build and deployment source to GitHub Actions.
